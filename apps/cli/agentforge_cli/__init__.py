"""AgentForge command-line interface.

Brings AgentForge's Quick Review (and task submission) into the terminal so it
can run where developers already live â€” pre-commit hooks, CI, scripts â€” instead
of only the web UI (FEATURE_GAP "CLId + IDE entry point").
"""

__version__ = "0.1.0"
