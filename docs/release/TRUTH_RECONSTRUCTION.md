# Truth Reconstruction

`Version: 1.0` · `Audit Focus: Feature Completeness & Production Readiness`

This document details the REAL implementation state of AgentForge features, verified directly by auditing backend and frontend source code.

---

## 1. Feature Inventory & Readiness Scorecard

| Feature Area | Claimed Behavior | Actual Code Behavior | Status Classification | Evidence & Context |
| :--- | :--- | :--- | :--- | :--- |
| **Authentication & Session Flow** | Secure password hashing, token validation, rotating refresh tokens, and auto-refresh on 401. | Implemented via standard JWT, bcrypt for hashing, database refresh token storage, and client auto-token requests. | **FULLY_IMPLEMENTED** | `app/auth.py`, `app/routes/auth.py`, `migrations/015_refresh_tokens.sql`, and `lib/api.ts` (Next.js client refresh interceptor). |
| **Orchestrator LangGraph Pipeline** | Agent workforce executing planning, building, review, testing, and delivery. | Spawns background asyncio workflows traversing a compiled LangGraph of nodes. Updates are logged to Postgres and broadcast via WebSockets. | **FULLY_IMPLEMENTED** | `apps/api/agents/graph.py`, `apps/api/agents/orchestrator.py`, and `core/task_tracker.py` execution thread pool. |
| **Docker Sandbox Executions** | Run untrusted code generated by Builder inside Docker containers with resource caps and custom seccomp profiles. | Uses `docker-py` SDK to run python/js scripts in docker containers with seccomp limits. Bypasses to standard subprocess execution if Docker is unavailable. | **FULLY_IMPLEMENTED** | `app/services/sandbox_executor.py` and `app/services/test_executor.py` (which runs pytest/jest in container environments). |
| **Agent Node Core Logics** | Highly complex multi-agent model coordination for Architect, Security, and DevOps. | Standard nodes in `agents/nodes/` evaluate basic LLM templates. Validation nodes (`evidence_validator_node`) generate static validation reports rather than deep LLM checks. | **PARTIALLY_IMPLEMENTED** | `agents/nodes/evidence_validator_node.py` (line 41: "Prepare inputs. For now, we'll create a placeholder validation"). |
| **Repository Intelligence Contexts** | Index full workspaces, parse symbols/imports, compile knowledge graphs for project context. | Code indexing and parsing is fully coded inside `app/repository_intelligence/`. Chunks code and saves symbols in PostgreSQL. | **FULLY_IMPLEMENTED** | `app/repository_intelligence/` parser files, and `migrations/007_repository_context.sql`. |
| **Static Review & Feedback Log** | Scan static files for security code smells and record user acceptance feedback. | Endpoints scan source code files, detect language, and log accept/reject decisions to PostgreSQL tables. | **FULLY_IMPLEMENTED** | `app/routes/review.py` and `app/routes/feedback.py`. |
| **Real-time Log Streaming** | Streaming token logs dynamically via WebSockets. | Broadcasts JSON payload events when an agent node starts/finishes or writes messages. It is not raw token-by-token character streaming. | **PARTIALLY_IMPLEMENTED** | `app/routes/tasks.py` WebSocket handler and `apps/api/agents/orchestrator.py` message callbacks. |

---

## 2. Code Evidence of Mocks and Placeholders

### 2.1 Evidence Validator Mocking
Inside `apps/api/agents/nodes/evidence_validator_node.py:L41-45`:
```python
    # Prepare inputs. In a real implementation, we would extract evidence packages from the state
    # For now, we'll create a placeholder validation based on existing outputs
```
And lines 98-106 populate hardcoded validation details with static success thresholds, bypassing complex semantic verification logic.

### 2.2 Docker Sandbox Fallback Path
Inside `apps/api/app/services/sandbox_executor.py:L15-20`:
```python
try:
    import docker
    from docker.errors import ImageNotFound
    DOCKER_AVAILABLE = True
except ImportError:
    DOCKER_AVAILABLE = False
```
If the Docker engine is missing or unavailable, the sandbox falls back to the host process execution, which poses a security boundary risk in environments where Docker is not configured.
