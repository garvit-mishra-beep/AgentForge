Analyze the following code. Identify:
1. What the code does (1 sentence)
2. External dependencies / imports used
3. Data flow (inputs → processing → outputs)
4. Potential problem areas (error handling, security, edge cases)

Code:
```
{{ code }}
```

Output JSON only:
{
  "purpose": "one sentence summary",
  "language": "detected language",
  "dependencies": ["dep1", "dep2"],
  "data_flow": "description of data flow",
  "problem_areas": ["area1", "area2"]
}
